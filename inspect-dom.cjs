const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch({headless: 'new'}); const page = await browser.newPage(); await page.goto('http://localhost:5173', {waitUntil: 'networkidle2'}); const html = await page.evaluate(() => { return 'ENQUIRY: ' + document.querySelector('a[href="enquiry.php"]').parentNode.outerHTML + '\\n\\nTRANSLATE: ' + document.getElementById('google_translate_element').outerHTML; }); require('fs').writeFileSync('dom-dump.txt', html); await browser.close(); })();
